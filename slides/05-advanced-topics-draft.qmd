---
title: "[**U**]{.flatly-green} [got this!]{.blk}"
subtitle: "Math and Programming Camp Day 5 — Session 2: Intro to Advanced Topics"
author: Parushya
date: today
---

<!--
DRAFT. Day 5 Session 2 (Intro to Advanced Topics). Created 2026-08-20
alongside 05-linear-algebra-draft.qmd, which is now Day 5 Session 1. This
file supersedes the stub slides/05-advanced-topics.qmd; sync into that file
once the content settles.

This is the camp's closing session and it is a BRIDGE, not a new course.

RESTRUCTURED 2026-08-20. The first draft carried seven blocks / ~35 slides,
far too much for a closing bridge session. Cut to three teaching blocks plus
the map:

  1. From Population to Sample   2. Frequentist and Bayesian   3. Causality

DELIBERATELY CUT -- do not re-add without a conversation:
  - The standalone "Estimand, Estimator, Estimate" block. It is now FOLDED
    INTO block 1, where it names the rungs of the population/parameter ->
    sample/statistic ladder rather than standing on its own.
  - "Two Facts, By Demonstration" (LLN and CLT, with two .viz placeholders).
    The CLT now survives only as a named clause on the interval slide -- we
    borrow the 1.96 from it, we do not teach it. HANDOFF #6 wanted these by
    simulation; simulation is a better fit for the R companion than for the
    last hour of the last day.
  - "Standard Errors and Confidence Intervals" as a block. Reduced to ONE
    slide, the Example stage of block 1. That slide is load-bearing and must
    not be cut: HANDOFF #7 moved frequentist-vs-Bayesian to Friday precisely
    so students argue about an interval they have already built. No interval
    on screen, no block 2.
  Note this departs from schedule.qmd / syllabus.qmd, which have been
  updated to match.

Everything is worked through the MEAN, not through beta-hat: mu = E[X] is the
parameter, X-bar is the estimator, the printed number is the estimate. Day 3's
expectation algebra is the only machinery needed. Keep it simple -- beta and
the D notation come back only in block 3, where we are quoting what their
October courses will actually write.

SECOND PASS 2026-08-20. Block 1 was rebuilt again, at request, to start from
the ground up and stay conceptual:

  Why: Populations and Parameters   (population -> parameter -> sample ->
                                     statistic -> estimator -> estimate;
                                     Greek vs. hat convention, in a table)
  The Estimand                      (the PHILOSOPHICAL move: a parameter is a
                                     fact about a population, an estimand is a
                                     fact about your question; they coincide
                                     only sometimes)
  Intuition: Two Worlds             (the visual, now AFTER the concepts, and
                                     carrying "when is the estimand simply the
                                     parameter?")
  Why We Imagine Repeating the Study (repeated sampling as a CONCEPT, the CLT
                                     as what makes it usable, and why honest
                                     studies produce overlapping intervals)

NINTH PASS 2026-08-21. Decomposition rebuilt.
  - The three-term equation (ATE + baseline bias + differential effect bias)
    was too complex AND DID NOT RENDER: \textcolor{#hex} inside $$...$$ fails
    under Quarto's MathJax. DO NOT COLOUR MATH WITH \textcolor OR \color.
    Colour comes from pandoc spans instead -- [$E[Y(1)|X=1]$]{.obs} -- which
    MathJax inherits via currentColor. Same trick as [?]{.po-miss}.
  - ONE colour scheme now runs through the whole block: .obs BLUE #2b6cb0 =
    we observe it; .unobs RED #B23A48 = it never happened, so we never see it.
    Every estimand is read off its colours; ATT is "half observed" and that is
    the point of that slide.
  - The decomposition is now the add-and-subtract shown in three short lines
    (what we compute / same thing in potential outcomes / what we want),
    followed by "The Bias Is That One Red Term".
  - tau is now EXPLAINED, on "Now the Notation": it is just the Greek letter
    for a causal effect, as mu was for a mean, and like mu it is a parameter.
  - Base font dropped 0.92em -> 0.88em to keep these slides inside the frame.

EIGHTH PASS 2026-08-21. Causality block extended into a full arc:
  - "Now the Notation" split (it overflowed): the mapping table stays, the
    fundamental-problem bullets move to their own slide.
  - New "Y and X: The General Notation" -- X treatment (binary OR continuous),
    Y outcome, Y(1)/Y(0) the POTENTIAL outcomes, W covariates. The deck
    switches from G/D to Y/X here and says so explicitly.
  - The decomposition is now step-wise. One base equation
    (SDO = ATE + baseline bias + differential effect bias, aligned over three
    lines) is REPRINTED on each of the next slides with the focal term in
    #B23A48 and the rest dimmed to #A9B2BD via \textcolor. If those colours
    ever stop rendering, the \underbrace labels still carry the meaning.
  - Own slides for: Baseline Bias, Selection Bias (the MECHANISM -- selection
    on levels produces the baseline term, selection on gains produces the
    third term; note that many texts call the baseline term "selection bias"),
    ATE, ATT, ATC, CATE.
  - "Why Some Designs Escape the Problem" defines EXOGENOUS vs endogenous in
    words before the formal (Y(1),Y(0)) independent of X.
  - Five design slides, two lines each plus a small inline SVG: randomised
    experiments, difference-in-differences, regression discontinuity,
    instrumental variables, matching. Figures use .design-fig and the standard
    palette. Keep them at two lines -- they are signposts, not a course.
  - "Where This Week Reappears" (the map table) REMOVED at request.

SEVENTH PASS 2026-08-21. Causality block reopened from the top:
  - "The Estimate We Have" now DEFINES its terms (G, D, E[G|D=1], E[G|D=0])
    instead of assuming them, and says what the comparison does give.
  - Split off "Why That Is Not Yet a Cause": the two obstacles in PLAIN WORDS,
    no notation -- the groups were already different (baseline), and regimes
    are not handed out at random (selection). "The Decomposition" later shows
    these are the same thing formally, so do not add notation here.
  - GROWTH IS NOW G, NOT Y, throughout the causality block, with a one-line
    note that textbooks write Y. Kept consistent on the block-1 estimand line.
  - The full table now reveals COLUMN BY COLUMN (raw HTML, shared
    data-fragment-index per column, .po-table) with plain-English headers and
    no notation. Notation arrives two slides later, on "Now the Notation".
  - Countries are Norway / Denmark / Saudi Arabia / North Korea.
  - NUMBERS CHANGED so the visible comparison gets the SIGN wrong, not just
    the magnitude: true average effect +0.125, visible comparison -1.55. That
    is the strongest version of the point and it feeds "The Decomposition".
    Keep that relationship if the values are ever edited.
  - "Formal: Potential Outcomes" is gone; "Now the Notation" replaces it and
    maps each plain-English column onto its symbol.

SIXTH PASS 2026-08-21. Added the potential-outcomes ("science") table to the
causality block, as two slides before "Formal: Potential Outcomes":
"If We Could See Both Columns" (the complete table, effects computable) and
"What We Actually See" (counterfactual cells blanked to a red "?"). The
contrast between the two is the point -- keep the columns identical so nothing
shifts between slides. "Formal: Potential Outcomes" was trimmed to match; it
no longer restates "you see Y(1) or Y(0), never both", which the tables now
show, and instead names tau = E[Y(1)-Y(0)] as the estimand.

THE NUMBERS ARE ILLUSTRATIVE AND MUST STAY THAT WAY -- the deck was
deliberately de-anchored from real dataset values in an earlier pass. They are
chosen so the arithmetic teaches: true average effect -0.325, naive visible
comparison -3.85, i.e. off by an order of magnitude. That gap IS the
selection-bias term named two slides later on "The Decomposition", so the two
slides reinforce each other. If the values are ever edited, keep that
relationship intact.

FIFTH PASS 2026-08-20 -- PREREQUISITE AUDIT. Checked line by line against
what the earlier sessions actually deliver (chapters/0N/programming.qmd and
slides/0N-*.qmd), not against what was planned. Findings and corrections:

  - NO REGRESSION HAS EVER BEEN RUN IN R. No lm(), no confint(), no broom, no
    model summary, in any programming chapter. Day 4 reaches
    geom_smooth(method = "lm") -- a line on a scatterplot, no coefficient
    table. So the deck may NOT say "R prints an interval next to every
    estimate". Fixed on "The Same Interval, Two Readings".
  - NO STANDARD ERROR IS EVER COMPUTED, by hand or in R. "SE [TODO]" removed
    from "The Estimate We Have".
  - BETA-HAT IS NEVER DERIVED. Day 4's agenda advertises "The Join --
    deriving bivariate beta-hat from a minimization criterion", but that deck
    stops after "What a Derivative Is"; there is no optimization/FOC section.
  - VAR(XBAR) = SIGMA^2/N IS NEVER DERIVED ANYWHERE. A previous pass sent
    students off to redo it "from Day 4"; Day 4 does not have it. Fixed.
  - THE TYPE-SHIFT IS DELIBERATELY NOT TAUGHT. slides/03-uncertainty-extended
    -and-expected.qmd says so in an explicit comment: E[Y|X] as a random
    variable and Adam's Law are out of scope there. Claims to the contrary
    removed. NOTE: HANDOFF calls the type-shift one of the two load-bearing
    moves of the week, so it is currently taught NOWHERE.
  - SAFE TO ASSUME IN R: mean(), var(), sd(), quantiles, summary(), table(),
    aggregate()/group-wise means, factors, data frames, subsetting,
    conditionals, dplyr verbs, pipes, ggplot + geom_smooth, set.seed()/rnorm()
    in passing.
  - DATASETS SEEN IN R ARE JPRdata_select.csv, COW country codes, and mpg --
    NOT V-Dem. Polyarchy and growth exist for students only as slide examples
    (a 20-country toy world on Days 1-2, percentile bins on Day 3). Keep the
    running example CONCEPTUAL; do not write as though they have the V-Dem
    cross-section loaded in front of them.

FOURTH PASS 2026-08-20. The parameter/statistic naming table moved to its own
slide ("Greek and Hats"); the estimator/estimate bullet moved onto "Estimand,
Estimator, Estimate"; and "Why We Imagine Repeating the Study" was CUT as
confusing. CONSEQUENCE: the deck no longer builds the sampling distribution,
the standard error, or the interval at all -- block 2 now introduces [2.1, 3.3]
as something R hands you, which students have seen in regression output. If
block 2 ever feels ungrounded on the day, that missing slide is why.

THIRD PASS 2026-08-20. Overflow fixes and a proper opening for block 2:
  - "The Estimand" split into two slides (the parameter-vs-estimand
    philosophy, then estimand/estimator/estimate + unbiased/consistent).
  - "Intuition: Two Worlds" split into two: the figure alone, then "When Is
    the Estimand Simply the Parameter?" (representativeness / measurement /
    descriptive-not-causal -- three assumptions, none checkable from data).
  - "Why This Argument Is Worth Having Now" CUT; it was doing the same work
    twice and confusing the handoff into block 2.
  - The "# Frequentist and Bayesian" divider is BACK (the previous pass's
    asymmetry is reversed), and block 2 now opens by naming what students
    have been doing unknowingly: "Everything So Far Has Been Frequentist",
    then "How Bayesians Differ", then "The Coin That Splits Them".
  - Those three slides draw on the 2025 deck, Resources/Slide Decks 2025/
    Latex_materials_mc/Latex/Slides/Math Camp Slides - Probability/main.tex,
    frames "Frequentist vs. Bayesian" (constant-but-unknown vs. parameter-as-
    random-variable; "confident" vs. "an opinion I update"; truth vs.
    prediction and perspective; academia leans frequentist for causal
    identification) and its already-flipped-coin example, which is the
    cleanest philosophical wedge in the archive. Reuse it verbatim in spirit.
  - "The Split, Precisely" became "The Same Interval, Two Readings", narrowed
    to confidence vs. credible on [2.1, 3.3] so it no longer duplicates the
    general philosophy now taught one slide earlier.

ALSO CUT in this pass -- deliberate, do not restore silently:
  - "Formal: Three Names for Three Things" (three-box SVG) and "Formal:
    Notation and Vocabulary". Both were reading as unmotivated formalism
    arriving out of nowhere; their content is now built up in prose across
    the first two slides.
  - "Intuition: Repeat the Study" (the population -> samples -> histogram
    SVG). Too much machinery for the point being made; replaced by the plain
    conceptual slide above.
  - "Formal: The Distribution of an Estimator".
  - The READ-ALONG DERIVATION of Var(Xbar) = sigma^2/n, and its find-the-error
    drill. NOTE THE COST: HANDOFF's "The goal, as settled" makes reading a
    variance derivation line by line the stated goal of the week, and this was
    the last place in the camp where that peaked. The Day 4 deck must now
    carry it alone, or the goal goes unmet.
  - "Example: Putting a Width on the Number" and "Quick Drill: Name the
    Object". The interval survives as the last bullet of the repeated-samples
    slide, because block 2 is unusable without one on screen.
  - The "# Frequentist and Bayesian" section divider, so that block now opens
    straight into "Why This Argument Is Worth Having Now". Causality and The
    Map still have dividers -- this is a deliberate asymmetry, not an
    oversight.

The dialect equivalence -- E[eps|X]=0 and "the selection-bias term is zero"
as one claim in two notations -- is the payoff of the entire week
(HANDOFF, "two load-bearing conceptual moves"). Do not compress it.

Running example is the standing one (RUNNING_EXAMPLE.md): V-Dem
cross-section, X = polyarchy 2000, Y = mean annual log GDP p.c. growth
2000-2019, D = 1{polyarchy > 0.5}, n ~ 175.

Block 1 now OPENS FROM THE BASICS -- population/parameter vs sample/statistic,
Greek vs hat, with age and income as throwaway illustrations before the V-Dem
growth mean takes over on the "Three Names" slide. The old opener ("we have
every country -- a standard error of WHAT?", with its superpopulation /
model-based / measurement trichotomy) was CUT 2026-08-20 as too sharp an
entry point. If it is wanted back it belongs AFTER "Repeat the Study", once
students know what a sampling distribution is, not before.

TODO(draft): print the real n, the real xbar/s/SE, and the real D-gap from
the V-Dem slice on every slide that currently shows a placeholder number.
Placeholders in use: n = 175, xbar = 2.7, SE = 0.31, CI = [2.1, 3.3].

Arc: Why -> Intuition -> Formal -> Example -> Quick Drill per topic block;
stage carried by the slide title, not an on-slide label.
Palette: project_slide_figure_palette.
-->

```{=html}
<style>
/* Deck-local density controls. This deck runs long and text-heavy; these
   trim type and leading a notch below the shared slides.css defaults. */
.reveal .slides section        { font-size: 0.88em; line-height: 1.26; }
.reveal .slides section h2     { font-size: 1.30em; margin-bottom: 0.35em; }
.reveal .slides section ul li,
.reveal .slides section ol li  { margin-bottom: 0.22em; }
.reveal .slides section ul ul,
.reveal .slides section ul ol  { font-size: 0.90em; }
.reveal .slides section table  { font-size: 0.88em; }
.reveal .slides section .columns { font-size: 0.96em; }
/* Potential-outcomes table: observed cells vs. the counterfactuals that were
   never recorded. Palette per project_slide_figure_palette. */
.reveal .slides .po-miss { color: #B23A48; font-weight: 700; }
.reveal .slides .po-obs  { color: #2b6cb0; }
.reveal .slides .po-table { font-size: 0.78em; margin: 0.4em auto; border-collapse: collapse; }
.reveal .slides .po-table th,
.reveal .slides .po-table td { padding: 0.24em 0.75em; text-align: center; border: none; line-height: 1.3; }
.reveal .slides .po-table th { border-bottom: 1px solid #5A6B80; font-weight: 700; }
/* Headings are #107F69 via slides.css; .blk forces part of one back to black. */
.reveal .slides .blk   { color: #000; }
.reveal .slides .obs   { color: #2b6cb0; }
.reveal .slides .unobs { color: #B23A48; }
.reveal .slides .eqline { text-align:center; margin: 0.30em 0; font-size: 1.02em; }
.reveal .slides .eqlab  { text-align:center; margin: 0.10em 0 0.45em; font-size: 0.80em; color:#5A6B80; }
.reveal .slides .legend { text-align:center; font-size: 0.80em; color:#5A6B80; margin-bottom: 0.5em; }
.reveal .slides .design-fig { display:flex; justify-content:center; margin: 2px 0 6px; }
.reveal .slides .design-fig svg { width:100%; max-width:660px; height:auto; }
.reveal .slides .po-table th:first-child,
.reveal .slides .po-table td:first-child { text-align: left; }
</style>
```

## Five Days, One Question {.smaller}

::: {.incremental}
-   Monday we asked: *does democracy cause economic growth?*
-   Since then we built the machinery to even state the question:
    -   **Day 1** — functions, $\sum$, logs: what a variable *is*.
    -   **Day 2** — probability, random variables, distributions.
    -   **Day 3** — counting, random variables, expectation, conditional expectation.
    -   **Day 4** — limits, derivatives, integrals: reading the symbols and saying what each one does.
    -   **This morning** — the matrix notation your methods courses write all of it in.
-   You can now *read* the formula that produces a number. **Nobody has yet said how sure you should be of one.**
:::

## This Afternoon {.smaller}

::: {.incremental}
-   **From population to sample** — parameter and statistic, estimand and estimator and estimate, and what has to be true for one to speak for the other.
-   **Two ways to talk about the same interval** — frequentist and Bayesian.
-   **Why none of it answers Monday's question** — causality, the gap more data cannot close, and the designs built to close it.
-   Then **the map**: where each of this week's tools shows up in your courses.
:::

# From Population to Sample

*Where the uncertainty was hiding all week.*

## Why: Populations and Parameters {.smaller}

::: {.incremental}
-   A **population** is *all* the values there are — every unit you care about, not a selection of them.
-   A **parameter** is one number that describes that whole population.
    -   The mean age of every adult; the mean income of every household; the mean growth rate of every country.
-   Parameters are **fixed** — not random, they simply *are* — and **unknown**, because knowing one would mean measuring everybody.
-   So you take a **sample**, and on it you compute a **statistic**: the same kind of number, from the rows you actually have.
:::

## Greek and Hats: The Naming Convention {.smaller}

| | Population parameter | Sample statistic |
|---|---|---|
| mean | $\mu$ | $\bar x$  (i.e. $\hat\mu$) |
| standard deviation | $\sigma$ | $s$  (i.e. $\hat\sigma$) |
| proportion | $\pi$ | $\hat p$ |

::: {.fragment}
**Greek for what you want; a hat, or a Roman letter, for what you computed.** The hat means *estimated from data*.
:::

## The Estimand: What You Actually Want to Know {.smaller}

::: {.incremental}
-   A parameter is a fact about a **population**. An **estimand** is a fact about **your question**. These are not the same kind of thing.
-   The estimand is chosen *before* the data, by you, out of the research question. The parameter is just whatever the population happens to have.
-   Sometimes they coincide — and sometimes no parameter of your data is your estimand at all:
    -   *Question:* mean growth of all countries. *Estimand* $= \mu$, a parameter of the population you sampled. They coincide.
    -   *Question:* the **effect** of democracy on growth.
        -   *Estimand* $= E[G(1)-G(0)]$ — about worlds that never happened. No mean of any dataset equals it.
:::

## Estimand, Estimator, Estimate {.smaller}

::: {.incremental}
-   Used as a guess at the parameter, the recipe is an **estimator**; the number it returns on your data is an **estimate**.
-   Three words, three **types**. Keeping them apart is most of the discipline:
    -   **Estimand** $\theta$ — what you want. Fixed, unknown, *chosen*.
    -   **Estimator** $\hat\theta = g(X_1,\dots,X_n)$ — the recipe. A **random variable**, because the data are.
    -   **Estimate** $\hat\theta_{\text{obs}}$ — the number, `2.7`. Not random; it already happened.
-   Two properties you will hear constantly, both about the **middle** one:
    -   **Unbiased:** $E[\hat\theta] = \theta$ — right *on average across samples*, not right in yours.
    -   **Consistent:** $\hat\theta \to \theta$ as $n \to \infty$.
-   Keeping them apart is what makes a results sentence precise.
    -   *"The estimate is 2.7"* reports this sample. *"The effect is 2.7"* is a claim about the estimand.
    -   Getting from the first to the second takes assumptions, and they should be stated.
:::

## Intuition: Two Worlds {.smaller}

```{=html}
<div style="display:flex; justify-content:center; margin: 4px 0;">
<svg viewBox="0 0 700 200" style="width:100%; max-width:900px; height:auto;" xmlns="http://www.w3.org/2000/svg">
  <text x="150" y="16" text-anchor="middle" font-size="13" font-weight="700" fill="#22303F">THE POPULATION</text>
  <ellipse cx="150" cy="100" rx="132" ry="76" fill="#EDF0F5" stroke="#22303F" stroke-width="1.5"/>
  <text x="150" y="84" text-anchor="middle" font-size="15" font-style="italic" fill="#B23A48">μ<tspan font-size="10" dy="4">age</tspan><tspan dy="-4"> = ?</tspan></text>
  <text x="150" y="110" text-anchor="middle" font-size="15" font-style="italic" fill="#B23A48">μ<tspan font-size="10" dy="4">income</tspan><tspan dy="-4"> = ?</tspan></text>
  <text x="150" y="136" text-anchor="middle" font-size="15" font-style="italic" fill="#B23A48">μ<tspan font-size="10" dy="4">growth</tspan><tspan dy="-4"> = ?</tspan></text>
  <text x="150" y="192" text-anchor="middle" font-size="11" fill="#5A6B80">parameters · fixed · unknown · never seen</text>

  <path d="M290,86 L392,86 M383,79 L392,86 L383,93" fill="none" stroke="#5A6B80" stroke-width="2"/>
  <text x="341" y="74" text-anchor="middle" font-size="11" fill="#5A6B80">take a sample</text>

  <path d="M392,126 L290,126 M299,119 L290,126 L299,133" fill="none" stroke="#2f855a" stroke-width="2" stroke-dasharray="5,3"/>
  <text x="341" y="116" text-anchor="middle" font-size="11" fill="#2f855a">estimates</text>

  <text x="545" y="16" text-anchor="middle" font-size="13" font-weight="700" fill="#2b6cb0">THE SAMPLE</text>
  <rect x="402" y="30" width="286" height="140" fill="#F7F8FA" stroke="#2b6cb0" stroke-width="1.5"/>
  <text x="545" y="50" text-anchor="middle" font-size="11" fill="#8a8f98">n = 175 rows</text>
  <text x="545" y="84" text-anchor="middle" font-size="15" font-style="italic" fill="#2b6cb0">x̄<tspan font-size="10" dy="4">age</tspan><tspan dy="-4"> = 47.3</tspan></text>
  <text x="545" y="110" text-anchor="middle" font-size="15" font-style="italic" fill="#2b6cb0">x̄<tspan font-size="10" dy="4">income</tspan><tspan dy="-4"> = 12.4k</tspan></text>
  <text x="545" y="136" text-anchor="middle" font-size="15" font-style="italic" fill="#2b6cb0">x̄<tspan font-size="10" dy="4">growth</tspan><tspan dy="-4"> = 2.7</tspan></text>
  <text x="545" y="192" text-anchor="middle" font-size="11" fill="#5A6B80">statistics · computed · known · they move</text>
</svg>
</div>
```

::: {.fragment}
Solid arrow: you draw a sample. Dashed green arrow: the statistic is used to **estimate** the parameter. That green arrow is the whole subject.
:::

## When Is the Estimand Simply the Parameter? {.smaller}

::: {.incremental}
-   The green arrow claims $\bar x$ speaks for $\mu$.
    -   It speaks for **what you wanted** only under three conditions — all assumptions, none checkable from the data alone:
    -   **Representativeness** — the population you sampled *is* the population your question is about.
    -   **Measurement** — the thing you measured *is* the thing you meant. (Polyarchy is itself an estimate, with V-Dem's own credible intervals.)
    -   **Descriptive, not causal** — the question asks what *is*, not what *would have been*.
-   Break any one and $\bar x$ still estimates $\mu$ perfectly well. It just stops estimating **your estimand**.
-   Our data breaks the third one, badly. That is the last block of the afternoon.
:::

# Frequentist and Bayesian

*You have been doing one of these all afternoon without being told.*

## Everything So Far Has Been Frequentist {.smaller}

::: {.incremental}
-   Look back at what we just built. Every move rested on one commitment: **the parameter is fixed; the data are random.**
    -   $\mu$ was "fixed · unknown · never seen" — never random.
    -   $\hat\theta$ was the random variable, because the *sample* could have come out otherwise.
    -   The three conditions were about whether $\bar x$ could be trusted to speak for a $\mu$ that sits still while we sample.
-   That commitment has a name.
    -   The **frequentist** school holds the parameter of interest to be **constant but unknown**.
    -   Probability is defined as **long-run frequency over repeated trials**.
-   Its characteristic sentence: *there is a truth, a right answer, and I can be **confident** to some degree that I have found it.*
-   Nothing forced this on us. It was smuggled in on the very first slide, when we called $\mu$ fixed.
:::

## How Bayesians Differ {.smaller}

::: {.incremental}
-   **Bayesian:** the parameter of interest is treated as a **random variable**.
    -   Not because $\mu$ varies — because probability is used to represent uncertainty *about* $\mu$.
-   Probability therefore attaches to **propositions** — including "$\mu$ lies in this interval" — and not only to repeatable events.
-   Both schools use the same machinery from Day 2. Write $H$ for a hypothesis about the parameter and $e$ for the evidence:
    $$p(H \mid e) \;=\; \frac{p(e \mid H)\,p(H)}{p(e)}$$
-   Nobody disputes this theorem. The schools differ over **which term is the object of inference**.
:::

## $p(e \mid H)$ and $p(H \mid e)$ {.smaller}

::: {.incremental}
-   **Frequentist inference works with $p(e \mid H)$** — the probability of the *evidence*, with the hypothesis held fixed.
    -   A $p$-value is $p(\text{evidence this extreme} \mid H_0)$.
    -   The coverage statement behind a confidence interval has the same shape: a probability about *intervals*, given a fixed $\mu$.
-   **Bayesian inference targets $p(H \mid e)$** — the probability of the *hypothesis*, given the evidence in hand.
-   These are different quantities. Reporting one and reading it as the other is the most common misreading in applied work.
:::

## The Prior Is What Converts One Into the Other {.smaller}

::: {.incremental}
-   The theorem states exactly what turns $p(e \mid H)$ into $p(H \mid e)$: the **prior** $p(H)$.
    -   Without $p(H)$, $\;p(e \mid H)$ does not become $p(H \mid e)$. There is no way around this.
-   So the disagreement is not about the algebra but about whether $p(H)$ is a defined object:
    -   **Frequentist:** $H$ is not random, so $p(H)$ has no referent — and neither does $p(H \mid e)$.
    -   **Bayesian:** $p(H)$ states what was held before the data, and the theorem updates it to $p(H \mid e)$.
:::

## The Coin That Splits Them {.smaller}

::: {.incremental}
-   *I am **about to** flip a fair coin. What is the probability it lands heads?*
    -   Frequentist: **50%.** Bayesian: **50%.** No disagreement.
-   *I have **already** flipped it, and not looked. What is the probability that **this** coin is heads?*
    -   Bayesian: still **50%** — my information hasn't changed, and probability describes my information.
    -   Frequentist: **it is no longer a matter of probability.** The coin is what it is. It is heads or it isn't.
-   Nothing about the coin changed between the two questions. What changed is whether there is anything left to be random.
:::

::: {.fragment}
**Now put $\mu$ where the coin is.** The sample has been drawn; the number has been computed. Is there anything left to be random?
:::

## The Same Interval, Two Readings {.smaller}

Every estimate you report will come with an interval attached — call it $[\,L,\,U\,]$. Where that width comes from is next term's business. Both schools accept the arithmetic, and disagree entirely about the sentence it licenses.

:::: {.columns}

::: {.column width="50%" style="font-size:0.82em;"}
**Frequentist — a *confidence* interval**

-   $\mu$ is fixed; the **interval** is the random thing.
-   "95% of intervals built this way cover $\mu$."
-   The 95% is a property of the **procedure**, across studies — not of the one interval you have.
-   You may **not** say $P(\mu \in [\,L,\,U\,]) = 0.95$.
:::

::: {.column width="50%" style="font-size:0.82em;"}
**Bayesian — a *credible* interval**

-   $\mu$ is the random thing; the data are what you conditioned on.
-   "Given prior and data, $P(\mu \in [\,L,\,U\,]) = 0.95$."
-   Exactly the sentence you wanted to say, and are now licensed to.
-   The price: you must state a **prior**, and the answer depends on it.
:::

::::

## Where It Actually Bites {.smaller}

::: {.incremental}
-   **With lots of data and a mild prior, the two agree numerically.** Most of the time the fight is about interpretation, not digits.
-   It bites when data are thin, when the prior is informative, or when you need a probability statement about a hypothesis for a decision.
-   Neither camp is a moral position, and you are not required to pick one this week.
    -   You *are* required to notice which one a paper's language is assuming.
-   **Neither one rescues you from the next block.** A credible interval around a biased estimate is a precisely-quantified wrong answer.
:::

## Quick Drill: Whose Sentence Is It? {.smaller}

::: {.incremental}
-   **(a)** "There is a 90% probability the effect is positive."
-   **(b)** "We reject $H_0$ at the 5% level."
-   **(c)** "With a weakly informative prior, the posterior mean is 2.7."
-   **(d)** "Under repeated sampling this estimator is unbiased."
-   **Answers:** (a) Bayesian. (b) frequentist. (c) Bayesian. (d) frequentist.
-   Now the harder one: which of these did you *assume* the regression output was telling you, before this afternoon?
:::

## In Practice: What Each One Costs {.smaller}

::: {.incremental}
-   **Frequentist:** the estimate is usually a closed form or a quick optimization.
    -   In R it is mostly built in — `lm()`, `glm()`, `t.test()`, `confint()` — and returns immediately.
-   **Bayesian:** the posterior $p(H \mid e)$ rarely has a closed form.
    -   It is **sampled rather than solved**, by **MCMC** (Markov chain Monte Carlo) — a long chain of draws whose histogram approximates the posterior.
    -   Computationally heavy — seconds to hours — and the chains must be checked for convergence before the answer means anything.
-   **Packages:**
    -   Frequentist — base `stats`, plus `sandwich` and `fixest`.
    -   Bayesian — `brms` and `rstanarm` (both take `lm()`-style formulas, Stan underneath), `MCMCpack`, `rjags`.
-   **To read:**
    -   Gelman & Hill, *Data Analysis Using Regression and Multilevel/Hierarchical Models*.
    -   McElreath, *Statistical Rethinking*; Gelman et al., *Bayesian Data Analysis*.
:::

# Causality

*Our unanswered question: does democracy **cause** growth?*

## The Estimate We Have {.smaller}

::: {.incremental}
-   Let $G$ be a country's growth rate, and let $D$ record its regime: $D=1$ for a democracy, $D=0$ for an autocracy.
-   Two quantities we already know how to compute:
    -   $E[G \mid D=1]$ — average growth **among the democracies**. Take the democracies, average their growth.
    -   $E[G \mid D=0]$ — average growth **among the autocracies**. Same move, other group.
-   Their difference is the number every paper on this question reports:
    $$E[G \mid D=1] - E[G \mid D=0]$$
-   **What it gives us:** an honest *description*. Democracies grew faster, or slower, than autocracies — by this much.
    -   Nothing more is needed to compute it. It is two group averages and a subtraction.
:::

::: {.fragment}
*(Your courses will write $Y$ where we are writing $G$. Same object.)*
:::

## Why That Is Not Yet a Cause {.smaller}

::: {.incremental}
-   A cause would mean: *these countries grew faster **because** they were democracies.* The comparison above cannot say that, for two reasons.
-   **The two groups were already different.**
    -   Richer, older states with settled institutions sit on one side; poorer states catching up sit on the other. They would not have grown alike even under identical regimes.
-   **Regimes are not handed out at random.**
    -   Countries that became democracies were the kind of country that becomes a democracy — and whatever made that happen may drive growth too.
-   Neither problem shrinks with more countries. Both are about *which* countries are in which group, not how many.
-   To see why, it helps to imagine a table we can never actually have.
:::

## If We Could See Both Columns {.smaller}

Imagine we could observe each country **twice** — once as a democracy, once not.

```{=html}
<table class="po-table">
<thead>
<tr>
<th class="fragment fade-in" data-fragment-index="1">Country</th>
<th class="fragment fade-in" data-fragment-index="2">Democracy?</th>
<th class="fragment fade-in" data-fragment-index="3">D</th>
<th class="fragment fade-in" data-fragment-index="4">Growth if democratic</th>
<th class="fragment fade-in" data-fragment-index="5">Growth if not</th>
<th class="fragment fade-in" data-fragment-index="6">Difference</th>
</tr>
</thead>
<tbody>
<tr>
<td class="fragment fade-in" data-fragment-index="1">Norway</td>
<td class="fragment fade-in" data-fragment-index="2">Yes</td>
<td class="fragment fade-in" data-fragment-index="3">1</td>
<td class="fragment fade-in" data-fragment-index="4">2.1</td>
<td class="fragment fade-in" data-fragment-index="5">1.4</td>
<td class="fragment fade-in" data-fragment-index="6">+0.7</td>
</tr>
<tr>
<td class="fragment fade-in" data-fragment-index="1">Denmark</td>
<td class="fragment fade-in" data-fragment-index="2">Yes</td>
<td class="fragment fade-in" data-fragment-index="3">1</td>
<td class="fragment fade-in" data-fragment-index="4">1.8</td>
<td class="fragment fade-in" data-fragment-index="5">1.5</td>
<td class="fragment fade-in" data-fragment-index="6">+0.3</td>
</tr>
<tr>
<td class="fragment fade-in" data-fragment-index="1">Saudi Arabia</td>
<td class="fragment fade-in" data-fragment-index="2">No</td>
<td class="fragment fade-in" data-fragment-index="3">0</td>
<td class="fragment fade-in" data-fragment-index="4">5.6</td>
<td class="fragment fade-in" data-fragment-index="5">6.4</td>
<td class="fragment fade-in" data-fragment-index="6">&minus;0.8</td>
</tr>
<tr>
<td class="fragment fade-in" data-fragment-index="1">North Korea</td>
<td class="fragment fade-in" data-fragment-index="2">No</td>
<td class="fragment fade-in" data-fragment-index="3">0</td>
<td class="fragment fade-in" data-fragment-index="4">0.9</td>
<td class="fragment fade-in" data-fragment-index="5">0.6</td>
<td class="fragment fade-in" data-fragment-index="6">+0.3</td>
</tr>
</tbody>
</table>
```

::: {.fragment}
The last column is, for each country, **the effect of democracy on its own growth** — what democracy did *for that country*. Average it and you have the effect of democracy, full stop: $(0.7 + 0.3 - 0.8 + 0.3)/4 = +0.125$.
:::

::: {.fragment}
*Illustrative values, not data.*
:::

## In the Real World, One Column Is Blank {.smaller}

A country is either a democracy or it isn't. We never see the other version of it.

```{=html}
<table class="po-table">
<thead>
<tr><th>Country</th><th>Democracy?</th><th>D</th><th>Growth if democratic</th><th>Growth if not</th><th>Difference</th></tr>
</thead>
<tbody>
<tr><td>Norway</td><td>Yes</td><td>1</td><td class="po-obs">2.1</td><td class="po-miss">?</td><td class="po-miss">?</td></tr>
<tr><td>Denmark</td><td>Yes</td><td>1</td><td class="po-obs">1.8</td><td class="po-miss">?</td><td class="po-miss">?</td></tr>
<tr><td>Saudi Arabia</td><td>No</td><td>0</td><td class="po-miss">?</td><td class="po-obs">6.4</td><td class="po-miss">?</td></tr>
<tr><td>North Korea</td><td>No</td><td>0</td><td class="po-miss">?</td><td class="po-obs">0.6</td><td class="po-miss">?</td></tr>
</tbody>
</table>
```

::: {.incremental}
-   Every [?]{.po-miss} is a number that was **never recorded anywhere**. Norway's growth without democratisation isn't missing from the dataset — it never happened.
-   So **not one row** of the difference column can be filled in. Not for any country, ever.
-   All we can do is compare the two visible columns: $\;\text{mean}(2.1,\,1.8) - \text{mean}(6.4,\,0.6) = -1.55$.
    -   The true answer, one slide ago, was $+0.125$. The visible comparison gets the **sign wrong**.
:::

## Now the Notation {.smaller}

The two columns you just saw have names, and you will meet them constantly.

| What we called it | Notation | Read it as |
|---|:--:|---|
| Growth if democratic | $G_i(1)$ | country $i$'s growth in the world where it *is* a democracy |
| Growth if not | $G_i(0)$ | the same country's growth in the world where it is *not* |
| Difference | $G_i(1) - G_i(0)$ | the causal effect of democracy **for country $i$** |
| Average of that column | $\tau = E[G(1) - G(0)]$ | the average causal effect — **our estimand** |

::: {.fragment}
$\tau$ ("tau") is just a **name**. It is the Greek letter conventionally used for a causal effect, exactly as $\mu$ was the letter for a mean — and like $\mu$ it is a **parameter**: one fixed, unknown number we are trying to learn.
:::

## The Fundamental Problem {.smaller}

::: {.incremental}
-   $G_i(1)$ and $G_i(0)$ are the **potential outcomes**. Both exist; only one is ever observed.
-   That is **the fundamental problem of causal inference**, and it is a *missing-data* problem.
    -   More countries add more rows. Every new row still arrives with one side blank.
-   Which is why $\tau$ is an estimand and not a statistic: no dataset contains the column it averages.
:::

## $Y$ and $X$: The General Notation {.smaller}

Democracy and growth were one instance. The general vocabulary:

::: {.incremental}
-   $X$ — the **treatment**, the thing whose effect you want.
    -   Often binary ($X=1$ treated, $X=0$ untreated): democracy, a job programme, a get-out-the-vote call.
    -   It can also be continuous — years of schooling, dose, a polyarchy score — in which case "the effect" means the effect of *changing* $X$.
-   $Y$ — the **outcome**, the thing you measure afterwards.
-   $Y_i(1)$ and $Y_i(0)$ — the ***potential outcomes***: unit $i$'s outcome if treated, and if not.
    -   The word *potential* is doing real work. Both are properties of the unit, whichever one the world happens to reveal.
-   $W$ — **covariates**, everything else you know about the unit before treatment.
:::

::: {.fragment}
From here on: $Y$ and $X$, not $G$ and $D$. Same objects, the notation your courses use.
:::

## What We Compute vs. What We Want {.smaller}

::: {.legend}
[blue = we observe it]{.obs}  ·  [red = it never happened, so we never see it]{.unobs}
:::

::: {.eqline}
**What we compute:**  [$E[Y \mid X{=}1]$]{.obs} $\;-\;$ [$E[Y \mid X{=}0]$]{.obs}
:::

::: {.fragment}
::: {.eqline}
**The same thing, in potential outcomes:**  [$E[Y(1) \mid X{=}1]$]{.obs} $\;-\;$ [$E[Y(0) \mid X{=}0]$]{.obs}
:::
:::

::: {.fragment}
::: {.eqline}
**What we want:**  [$E[Y(1) \mid X{=}1]$]{.obs} $\;-\;$ [$E[Y(0) \mid X{=}1]$]{.unobs}
:::
::: {.eqlab}
the same countries, treated and untreated
:::
:::

::: {.fragment}
The two lines differ in **one term only** — and it is the red one.
:::

## The Bias Is That One Red Term {.smaller}

::: {.eqline}
$\text{bias} \;=\;$ [$E[Y(0) \mid X{=}1]$]{.unobs} $\;-\;$ [$E[Y(0) \mid X{=}0]$]{.obs}
:::

::: {.incremental}
-   We wanted to subtract the red term. We subtracted the blue one instead, because it is the one that exists in the data.
-   So the number we compute is **the effect plus this gap**, and the gap has a name: **baseline bias**.
-   Read it in words: *how the two groups would have compared had neither been treated.*
    -   Norway and North Korea do not share a $Y(0)$. Democracy is not what makes them differ.
-   If the treated group would have done better anyway, the gap is positive and our number **overstates** the effect.
-   It contains a red term, so it can never be computed. No larger $n$ turns it blue.
:::

## Selection Bias {.smaller}

::: {.incremental}
-   **Selection** is the *mechanism* behind that gap: units are not assigned treatment, they end up with it.
-   Two ways it bites:
    -   **Selection on levels** — units that would have done better anyway are likelier to be treated. This is the baseline bias above.
    -   **Selection on gains** — units that would *benefit most* are likelier to be treated. Then the effect on the treated differs from the effect on everyone else.
-   Many texts use "selection bias" for the baseline term itself. Notice which sense is meant.
-   Both are about **which** units are in which group — never about how many.
:::

## ATE — Average Treatment Effect {.smaller}

::: {.eqline}
$\tau_{\text{ATE}} \;=\; E\big[\,Y(1) - Y(0)\,\big]$
:::

::: {.incremental}
-   The average effect **over everybody**, treated and untreated alike.
-   *If we treated the whole population instead of nobody, how far would the average outcome move?*
-   It is the default estimand — and frequently not the one a study can deliver.
:::

## ATT — Effect on the Treated {.smaller}

::: {.eqline}
$\tau_{\text{ATT}} \;=\;$ [$E[Y(1) \mid X{=}1]$]{.obs} $\;-\;$ [$E[Y(0) \mid X{=}1]$]{.unobs}
:::

::: {.incremental}
-   The average effect **among the units that were actually treated**.
-   *For the countries that did democratise, what did democratising do for them?*
-   Notice the colours: **one half is observed.** That is why observational work so often targets the ATT — you are missing one term, not two.
:::

## ATC — Effect on the Controls {.smaller}

::: {.eqline}
$\tau_{\text{ATC}} \;=\;$ [$E[Y(1) \mid X{=}0]$]{.unobs} $\;-\;$ [$E[Y(0) \mid X{=}0]$]{.obs}
:::

::: {.incremental}
-   The average effect **among the units that were not treated** — what treatment *would have* done to them.
-   *What would democratising the remaining autocracies do?* The policy question, and the harder one.
-   The two combine into the ATE, weighted by the share treated $\pi$:
    -   $\tau_{\text{ATE}} = \pi\,\tau_{\text{ATT}} + (1-\pi)\,\tau_{\text{ATC}}$
-   When $\tau_{\text{ATT}} \neq \tau_{\text{ATC}}$ the effect is **heterogeneous** — treatment does different things to different units.
:::

## CATE — Conditional Average Treatment Effect {.smaller}

::: {.eqline}
$\tau_{\text{CATE}}(w) \;=\; E\big[\,Y(1) - Y(0) \,\big|\, W = w\,\big]$
:::

::: {.incremental}
-   The average effect **within a subgroup** picked out by covariates $W$ — by region, income level, prior institutions.
-   It is not one number but a **function** of $w$: a different effect for each kind of unit.
-   This is where "does democracy cause growth?" ends up once people take it seriously.
    -   Not one effect for all countries, but an effect that depends on what the country already is.
-   Four estimands — ATE, ATT, ATC, CATE — from one table. **Choose before you estimate.**
:::

## Why Some Designs Escape the Problem {.smaller}

::: {.incremental}
-   Both bias terms come from one thing: **who ends up treated is related to what would have happened to them.**
-   A design escapes when treatment is assigned **exogenously** — from outside the system, by something unrelated to the potential outcomes.
    -   *Exogenous* = determined **outside** the process you are studying. *Endogenous* = determined **within** it.
    -   A country's regime is endogenous to its history, wealth and institutions. A coin flip is not.
-   Written down: assignment independent of the potential outcomes, $\;\big(Y(1), Y(0)\big) \perp X$.
    -   Then $E[Y(0)\mid X{=}1] = E[Y(0)\mid X{=}0]$, so baseline bias is zero; and $\tau_{\text{ATT}} = \tau_{\text{ATC}}$, so the third term is zero too.
-   The left side of the decomposition then **equals** the ATE. The statistic becomes the estimand.
-   Everything that follows is a catalogue of ways to get partway there when you cannot randomise.
:::

## Randomised Experiments {.smaller}

```{=html}
<div class="design-fig">
<svg viewBox="0 0 480 132" xmlns="http://www.w3.org/2000/svg">
  <text x="56" y="24" text-anchor="middle" font-size="11" fill="#22303F">all units</text>
  <rect x="8" y="32" width="96" height="64" fill="#EDF0F5" stroke="#22303F" stroke-width="1.3"/>
  <g fill="#5A6B80"><circle cx="30" cy="54" r="4"/><circle cx="56" cy="54" r="4"/><circle cx="82" cy="54" r="4"/><circle cx="30" cy="76" r="4"/><circle cx="56" cy="76" r="4"/><circle cx="82" cy="76" r="4"/></g>
  <path d="M108,64 L134,64 M127,58 L134,64 L127,70" fill="none" stroke="#5A6B80" stroke-width="1.5"/>
  <circle cx="160" cy="64" r="21" fill="#F7F8FA" stroke="#2f855a" stroke-width="1.5"/>
  <text x="160" y="70" text-anchor="middle" font-size="15" fill="#2f855a">&#9860;</text>
  <text x="160" y="104" text-anchor="middle" font-size="10" fill="#2f855a">randomiser</text>
  <path d="M186,53 L234,35 M226,30 L234,35 L227,42" fill="none" stroke="#5A6B80" stroke-width="1.5"/>
  <path d="M186,75 L234,93 M227,87 L234,93 L226,99" fill="none" stroke="#5A6B80" stroke-width="1.5"/>
  <rect x="238" y="10" width="118" height="44" fill="#F7F8FA" stroke="#2b6cb0" stroke-width="1.3"/>
  <text x="297" y="30" text-anchor="middle" font-size="11" fill="#2b6cb0">treated (X = 1)</text>
  <text x="297" y="45" text-anchor="middle" font-size="10" fill="#5A6B80">we observe Y(1)</text>
  <rect x="238" y="74" width="118" height="44" fill="#F7F8FA" stroke="#5A6B80" stroke-width="1.3"/>
  <text x="297" y="94" text-anchor="middle" font-size="11" fill="#5A6B80">control (X = 0)</text>
  <text x="297" y="109" text-anchor="middle" font-size="10" fill="#5A6B80">we observe Y(0)</text>
  <text x="424" y="58" text-anchor="middle" font-size="10" fill="#22303F">the groups differ</text>
  <text x="424" y="74" text-anchor="middle" font-size="10" fill="#22303F">only by chance</text>
</svg>
</div>
```

::: {.incremental}
-   **What it does:** a coin, not the unit, decides who is treated.
-   **Why it works:** assignment is independent of $Y(1)$ and $Y(0)$ by construction, so both bias terms are zero — not assumed away, *built* away.
:::

## Difference-in-Differences {.smaller}

```{=html}
<div class="design-fig">
<svg viewBox="0 0 480 132" xmlns="http://www.w3.org/2000/svg">
  <line x1="46" y1="110" x2="352" y2="110" stroke="#5A6B80" stroke-width="1.2"/>
  <line x1="46" y1="110" x2="46" y2="16" stroke="#5A6B80" stroke-width="1.2"/>
  <text x="86" y="125" text-anchor="middle" font-size="10" fill="#5A6B80">before</text>
  <text x="266" y="125" text-anchor="middle" font-size="10" fill="#5A6B80">after</text>
  <line x1="86" y1="88" x2="266" y2="76" stroke="#5A6B80" stroke-width="2"/>
  <line x1="86" y1="64" x2="266" y2="32" stroke="#2b6cb0" stroke-width="2"/>
  <line x1="86" y1="64" x2="266" y2="52" stroke="#2b6cb0" stroke-width="1.6" stroke-dasharray="5,3" stroke-opacity="0.55"/>
  <line x1="272" y1="32" x2="272" y2="52" stroke="#B23A48" stroke-width="2.5"/>
  <text x="282" y="46" font-size="10" fill="#B23A48">effect</text>
  <text x="276" y="26" font-size="10" fill="#2b6cb0">treated</text>
  <text x="276" y="80" font-size="10" fill="#5A6B80">control</text>
  <text x="150" y="48" font-size="9.5" fill="#8a8f98">counterfactual trend</text>
  <text x="366" y="102" font-size="9.5" fill="#8a8f98">parallel trends</text>
  <text x="366" y="116" font-size="9.5" fill="#8a8f98">is the assumption</text>
</svg>
</div>
```

::: {.incremental}
-   **What it does:** compares the *change* over time in a treated group against the change in an untreated one.
-   **Why it works:** fixed baseline differences cancel in the subtraction — provided the two groups would have moved in **parallel** absent treatment.
:::

## Regression Discontinuity {.smaller}

```{=html}
<div class="design-fig">
<svg viewBox="0 0 480 132" xmlns="http://www.w3.org/2000/svg">
  <line x1="46" y1="110" x2="420" y2="110" stroke="#5A6B80" stroke-width="1.2"/>
  <line x1="228" y1="14" x2="228" y2="110" stroke="#B23A48" stroke-width="1.5" stroke-dasharray="5,3"/>
  <text x="228" y="125" text-anchor="middle" font-size="10" fill="#B23A48">cutoff</text>
  <line x1="64" y1="88" x2="224" y2="70" stroke="#5A6B80" stroke-width="2"/>
  <line x1="232" y1="44" x2="404" y2="28" stroke="#2b6cb0" stroke-width="2"/>
  <line x1="228" y1="70" x2="228" y2="44" stroke="#B23A48" stroke-width="3"/>
  <text x="244" y="62" font-size="10" fill="#B23A48">jump = the effect</text>
  <text x="106" y="102" font-size="10" fill="#5A6B80">untreated</text>
  <text x="352" y="52" font-size="10" fill="#2b6cb0">treated</text>
  <text x="228" y="10" text-anchor="middle" font-size="9.5" fill="#8a8f98">units just either side are nearly identical</text>
</svg>
</div>
```

::: {.incremental}
-   **What it does:** compares units just above and just below a threshold that mechanically determines treatment.
-   **Why it works:** near the cutoff, which side you land on is as good as random — so the jump at the line is the effect, for units near the line.
:::

## Instrumental Variables {.smaller}

```{=html}
<div class="design-fig">
<svg viewBox="0 0 480 132" xmlns="http://www.w3.org/2000/svg">
  <circle cx="78" cy="80" r="20" fill="#EAF6EF" stroke="#2f855a" stroke-width="1.4"/>
  <text x="78" y="85" text-anchor="middle" font-size="14" fill="#2f855a">Z</text>
  <circle cx="212" cy="80" r="20" fill="#F7F8FA" stroke="#2b6cb0" stroke-width="1.4"/>
  <text x="212" y="85" text-anchor="middle" font-size="14" fill="#2b6cb0">X</text>
  <circle cx="350" cy="80" r="20" fill="#F7F8FA" stroke="#22303F" stroke-width="1.4"/>
  <text x="350" y="85" text-anchor="middle" font-size="14" fill="#22303F">Y</text>
  <circle cx="281" cy="26" r="18" fill="#FAEDEF" stroke="#B23A48" stroke-width="1.4"/>
  <text x="281" y="31" text-anchor="middle" font-size="13" fill="#B23A48">U</text>
  <path d="M100,80 L186,80 M178,74 L186,80 L178,86" fill="none" stroke="#5A6B80" stroke-width="1.6"/>
  <path d="M234,80 L324,80 M316,74 L324,80 L316,86" fill="none" stroke="#5A6B80" stroke-width="1.6"/>
  <path d="M267,42 L224,64 M231,58 L224,64 L232,69" fill="none" stroke="#B23A48" stroke-width="1.5" stroke-dasharray="4,3"/>
  <path d="M295,42 L338,64 M331,58 L338,64 L330,69" fill="none" stroke="#B23A48" stroke-width="1.5" stroke-dasharray="4,3"/>
  <text x="78" y="116" text-anchor="middle" font-size="9.5" fill="#5A6B80">instrument</text>
  <text x="212" y="116" text-anchor="middle" font-size="9.5" fill="#5A6B80">treatment</text>
  <text x="350" y="116" text-anchor="middle" font-size="9.5" fill="#5A6B80">outcome</text>
  <text x="404" y="30" font-size="9.5" fill="#B23A48">confounder</text>
  <text x="404" y="44" font-size="9.5" fill="#B23A48">(unobserved)</text>
</svg>
</div>
```

::: {.incremental}
-   **What it does:** finds a variable $Z$ that shifts treatment $X$ but touches the outcome **only through** $X$.
-   **Why it works:** the part of $X$ that $Z$ moves is unrelated to the confounder $U$ — so that part, and only that part, gives a clean comparison.
:::

## Matching {.smaller}

```{=html}
<div class="design-fig">
<svg viewBox="0 0 480 132" xmlns="http://www.w3.org/2000/svg">
  <text x="120" y="18" text-anchor="middle" font-size="11" fill="#2b6cb0">treated</text>
  <text x="330" y="18" text-anchor="middle" font-size="11" fill="#5A6B80">untreated pool</text>
  <g fill="#2b6cb0"><circle cx="120" cy="42" r="6"/><circle cx="120" cy="70" r="6"/><circle cx="120" cy="98" r="6"/></g>
  <g fill="#5A6B80" fill-opacity="0.45"><circle cx="330" cy="32" r="6"/><circle cx="330" cy="54" r="6"/><circle cx="330" cy="76" r="6"/><circle cx="330" cy="98" r="6"/><circle cx="330" cy="118" r="6"/></g>
  <g stroke="#2f855a" stroke-width="1.6" stroke-dasharray="4,3">
    <line x1="128" y1="42" x2="322" y2="32"/>
    <line x1="128" y1="70" x2="322" y2="76"/>
    <line x1="128" y1="98" x2="322" y2="98"/>
  </g>
  <text x="225" y="118" text-anchor="middle" font-size="10" fill="#2f855a">matched on W</text>
  <text x="424" y="66" text-anchor="middle" font-size="9.5" fill="#8a8f98">unmatched</text>
  <text x="424" y="80" text-anchor="middle" font-size="9.5" fill="#8a8f98">units dropped</text>
</svg>
</div>
```

::: {.incremental}
-   **What it does:** pairs each treated unit with untreated units that look the same on observed covariates $W$, then compares within pairs.
-   **Why it works — and when it doesn't:** it removes bias from what you *measured*. If anything **unobserved** drives selection, the bias survives, and no diagnostic will tell you.
:::

## The Dialect Equivalence {.smaller}

::: {.incremental}
-   Your econometrics course will state the key assumption as **exogeneity**: $\;E[\varepsilon \mid X] = 0$.
-   Your causal inference course will state it as **the selection-bias term is zero**.
-   **These are the same claim in two notations.** Different courses, different textbooks, different vocabulary, one idea.
-   In English, for our data: *whatever else drives growth is unrelated to how democratic a country is.*
    -   Say it out loud about the V-Dem cross-section and hear how implausible it is.
-   Recognizing this equivalence when you meet it in October is the single most useful thing this week can give you.
:::

## Quick Drill: Which Gap? {.smaller}

::: {.incremental}
-   Three gaps stand between us and Monday's question. Which one does each fix close?
    -   **(a)** Collect 30 more countries.
    -   **(b)** Randomly assign democracy to countries.
    -   **(c)** Use V-Dem's credible intervals instead of the point estimate for polyarchy.
    -   **(d)** Add region and prior-income controls.
-   **Answers:**
    -   (a) estimation only — narrower interval, same bias. (b) identification — and is unavailable, which is the point.
    -   (c) measurement. (d) *maybe* identification, conditional on an untestable assumption you now know how to state.
-   Only estimation closes with $n$. The other two don't, ever.
:::

# The Map

## What to Do in the Next Two Weeks {.smaller}

::: {.incremental}
-   **Don't re-read these slides.** Re-do the one derivation you have on blank paper.
    -   The selection-bias decomposition: four lines, linearity of expectation only. Naming the rule on each line is the skill.
-   Keep the find-the-error habit. When a paper shows a line, ask what licenses it.
-   Start the linear algebra series when it runs; you have the vocabulary now.
-   When something in October looks unfamiliar, check first whether it's a new idea or just new handwriting. Usually it's handwriting.
-   Ask us. Really.
:::

## References {.smaller}

::: {.incremental}
-   **IQSS Prefresher** (Harvard IQSS) — [Probability](https://iqss.github.io/prefresher/06_probability.html) and the LLN/CLT sections.
-   **Seeing Theory** (Brown University) — animated [frequentist inference](https://seeing-theory.brown.edu/frequentist-inference/index.html) and [Bayesian inference](https://seeing-theory.brown.edu/bayesian-inference/index.html); the CLT visualizer is the best free one, and is the place to go if today's 1.96 bothered you.
-   **3Blue1Brown** — [Central limit theorem](https://www.youtube.com/watch?v=zeJD6dqJ5lo), [Bayes' theorem](https://www.youtube.com/watch?v=HZGCoVF3YvM).
-   Cunningham, S. *Causal Inference: The Mixtape* — Ch. 4 (potential outcomes): [https://mixtape.scunning.com/](https://mixtape.scunning.com/)
-   Aronow, P. M., & Miller, B. T. (2019). *Foundations of Agnostic Statistics* — the estimand/estimator/estimate discipline, done rigorously.
-   Moore, W. H., & Siegel, D. A. (2013). *A Mathematics Course for Political and Social Research*.
:::

## [**U**]{.flatly-green} [got this!]{.blk} {.center}

**Programming lecture:** [Webpage Chapter](../chapters/05-advanced-topics/programming.html)

**Back to:** [Math Camp 2026 website](../index.html)

*Thanks for a good week.*
